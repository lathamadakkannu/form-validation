/*import React,{useState} from "react";

function Form(){

    const [change,setChange]=useState(0)


    const msgChange=(e)=>{
        setChange(e.target.value)
    }

    const msgSubmit=(e)=>{
        setChange(e.target.value)
    }
    return(

        <div>
            <h2>Hello{change}</h2>
            <input type="text" placeholder="Enter name" onChange={msgChange}  />

            <button onClick={msgSubmit}>Submit</button>
        </div>
    )
}

export default Form;*/